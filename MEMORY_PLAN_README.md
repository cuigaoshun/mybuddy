# 记忆后续计划

这份文档只保留当前仓库里“还没有完成”的记忆相关工作，已经落地的接入能力不再在这里重复描述。

## 已完成的部分

以下内容已经具备最小实现，因此不再作为计划项展开：

- `chat_memory` 已经存在，并承担当前消息级对话记忆存储。
- `ConversationMemoryService`、`ConversationMemoryRepository` 及其 PostgreSQL 实现已经存在。
- 用户消息与助手消息的消息级记忆写入已经接进主链路。
- `ChatSessionInfo` 会话级信息与回复租约机制已经存在。
- `user_memory` 表与用户级长期记忆读取链路已经有最小实现。
- Agent 图里已经通过 `retrieve_memory` 读取长期记忆，并在上下文格式化阶段注入系统消息。

因此，这份文档不再重复讨论：

- 如何新增 `chat_memory` 表。
- 如何定义 `MemoryRecord`。
- 如何引入 PostgreSQL + `pgvector`。
- 如何把消息记忆写入 `SessionManager`。
- 如何把 `im_sender` 改成目录结构。

## 当前还没完成的部分

当前记忆能力已经能完成“消息级写入 + 用户级长期记忆读取”的最小闭环，但还有几块仍然属于后续工作。

### 1. 长期记忆写入与更新链路

目前用户级长期记忆已经能读取，但还没有完整的更新闭环。

后续需要补的是：

- 从对话中提取长期有效信息。
- 把提取结果合并进 `user_memory`。
- 维护 `last_processed_message_id`、`version` 等字段。
- 控制冲突更新，而不是简单覆盖。

这里的重点不是“存更多字段”，而是补稳定的更新规则。

### 2. 用户画像提取策略

现在 `user_memory.user_profile_json` 已经有结构化类模型承接，但还缺少真正的提取策略。

后续需要明确：

- 哪些信息可以进入 `profile`。
- 哪些信息算长期偏好，进入 `preferences`。
- `relationship.affinity` 如何更新，避免单轮情绪把关系信号拉偏。
- 哪些内容不应该进入长期记忆。

### 3. 长期记忆摘要生成策略

当前上下文已经支持注入 `long_term_memory_summary`，但摘要本身还没有正式沉淀流程。

后续需要明确：

- 摘要是按覆盖式更新还是增量合并。
- 摘要长度控制在什么范围。
- 摘要和结构化画像之间如何分工。
- 摘要何时刷新，何时保持不动。

### 4. 异步整理与定时扫描

当前同步链路已经尽量保持轻量，因此更重的长期记忆整理不应该继续塞进主回复流程。

后续计划仍然是：

- 用 `APScheduler` 或等价机制定时扫描未处理会话。
- 基于 `last_processed_message_id` 做增量整理。
- 把重型记忆抽取、重算和补偿放进 `workers` 或后台任务层。

这部分现在仍然只停留在方案层，没有代码实现。

### 5. 记忆召回质量优化

当前消息级召回和长期记忆读取已经能工作，但还没有做更深入的质量优化。

后续可以考虑：

- 长期记忆摘要与消息级证据的优先级控制。
- 更稳定的重排策略。
- 针对陪伴场景的长期记忆筛选规则。
- 上下文注入长度与噪声控制。

### 6. Repository 与服务层的扩展能力

当前长期记忆侧还是读优先的最小实现。

后续需要补的可能包括：

- `user_memory` 的 upsert/update 接口。
- 幂等更新能力。
- 更细粒度的查询接口。
- 后续如果长期记忆拆分为多表时的抽象延展。

## 当前建议的后续顺序

如果继续往下做，建议优先级如下：

1. 先补 `user_memory` 的写入/更新接口。
2. 再定义长期记忆提取与合并规则。
3. 再补异步整理触发与后台处理。
4. 最后再做召回质量优化和更复杂的长期记忆治理。

## 结论

当前仓库的记忆能力已经不再是“从零接入”，而是进入“补长期记忆更新与治理”的阶段。

所以这份计划文档后续只关注两类事情：

- 如何把 `user_memory` 从“可读”推进到“可稳定更新”。
- 如何把长期记忆整理从同步最小闭环推进到后台可持续运行。
