services:
  mybuddy:
    image: mybuddy
    container_name: mybuddy
    working_dir: /workspace
    ports:
      - "18800:8000"
    volumes:
      - ./config.toml:/workspace/config.toml:ro
      - ./logs:/workspace/logs
