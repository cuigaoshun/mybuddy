# AGENTS.md

本文件作用域覆盖整个仓库。

## 项目定位

这是一个陪伴型 Agent 项目。

当前仓库的重点不是泛化的聊天机器人框架，而是先把飞书场景下的最小闭环落稳：

- 通过飞书 `websocket` 接收用户消息。
- 把飞书事件转换成内部统一消息模型。
- 通过路由层串起记忆写入、上下文构建、Agent 回复生成和消息发送。
- 通过飞书 API 把回复发回去。
- 用 PostgreSQL 承担当前存储实现，但上层必须依赖抽象接口。

除非用户显式要求，不要擅自扩展到其他 IM 平台，也不要把项目描述成多平台通用框架。

## 当前架构边界

后续实现、重构或补文档时，必须遵守以下模块职责：

- `bootstrap`：负责应用启动、依赖装配、监听器启动与生命周期管理。
- `gateway`：负责外部 IM 平台事件接入与平台字段归一化。
- `event`：负责内部事件模型与事件总线分发。
- `router`：负责消息路由、会话识别、处理顺序和调用编排。
- `agent`：负责上下文构建、状态图、回复生成决策。
- `memory`：负责记忆写入、检索、沉淀、历史查询与相关抽象。
- `services/im_sender`：负责统一发消息能力，不向上层暴露飞书 SDK 细节。
- `workers`：负责异步任务与后台消费逻辑。

不要把后台任务逻辑塞进 `router`、`gateway` 或 `bootstrap`。如果某项能力天然属于异步消费、补偿、整理、摘要等后台流程，应落到 `workers` 或等价后台层。

## 飞书接入约束

- 飞书接收入口优先沿用当前 `websocket` 模式。
- 飞书原始事件要尽早转换成内部统一模型，避免业务层直接依赖飞书字段。
- 飞书 SDK 调用必须集中在网关接入和发送器实现附近，不要散落到业务模块。
- 飞书发送能力统一走 `app/services/im_sender/`，不要在 `router`、`agent`、`memory` 里直接拼飞书请求。
- 飞书接入代码优先参考 `tests/lark_on_bot.py` 与 `tests/lark_u_to_bot.py` 的示例方向。

## 存储约束

所有存储能力必须先定义抽象，再写 PostgreSQL 实现。

至少要把以下概念隔离成独立仓储接口、协议或等价抽象：

- 会话
- 消息
- 用户记忆
- 事件日志
- 发送记录

禁止把 SQL、事务细节、PG 方言能力直接泄露到 `agent`、`router`、`gateway` 等上层业务模块。

当前仓库已经有这类方向：

- `app/memory/repositories.py` 定义抽象协议。
- `app/memory/postgres_repository.py` 与 `app/memory/postgres_session_info_repository.py` 提供 PG 实现。

后续新增存储能力时，应继续延续这个边界，不要为了省事直接让业务层拼 SQL。

## 当前实现认知要求

在修改文档、架构说明或相关代码时，要基于当前仓库事实，而不是把项目写成“未来计划”：

- FastAPI 入口已经存在，位于 `app/main.py`。
- 应用生命周期内已经会初始化配置、数据库、向量模型、Agent Graph，并启动飞书监听。
- 飞书消息分发、会话处理、记忆仓储、统一发送器都已经有最小实现。
- `workers` 层仍处于预留为主的状态，不能把它描述成已经完整落地。

如果要描述“当前项目状态”，应明确区分“已经有最小实现”和“仍待后续补全”的部分。

## 代码风格

- 优先小步实现，避免一次性生成大段未经验证的代码。
- 保持现有目录职责稳定，不随意重命名目录。
- 新增代码必须贴合现有模块边界，不要把多层职责混在一个文件里。
- 命名、文档和必要注释优先使用清晰直白的中文语义。
- 修问题时优先修根因，不做与当前请求无关的顺手重构。

## 测试与验证约束

- 不要擅自新增单元测试体系。
- 如果需要示例或验证，优先复用已有 `tests` 中的飞书样例。
- 如果改动的是文档或架构描述，重点验证其是否与当前代码结构一致，而不是补测试。

## 文档要求

如果修改架构、一期范围、模块职责、接入方式或项目定位，需要同步更新 `README.md`，并确保 README 仍然能清楚回答这些问题：

- 这个项目当前在做什么。
- 一期做什么，不做什么。
- 飞书如何接入。
- Agent、记忆、RAG、存储分别放在哪一层。
- 为什么虽然当前使用 PostgreSQL，但仍然要求抽象化。

其中：

- `README.md` 更偏项目介绍、当前状态和开发理解入口。
- `AGENTS.md` 更偏实现边界、架构纪律和协作约束。
